

Do similar as the beginer one
- but more details examples of distributed system

# System design

- scaling for availability
- scaling for performance
- when data is needed in 2 places
  - mechanisms
    - replications
    - failover
  - choosing trade offs

## Examples to distributed systems
- TODO give some examples of real world
  - lambda, pod autoscaling? serverless
  - aurora? kafka? mongodb?


## ASG
- TODO add notes here
- show how compute scaling is done
- how to configure when to scale up and down
  - target tracking/step scaling/scheduled(predictable)
  - cooldown/warmup period (prevent thrashing; callbacks to user_data.sh)
  - connection draining/deregistration delay (graceful shutdown) 
- how ASG detects and replaces unhealthy instances
  - show self-healing features
  - TODO need /health endpoint, should we provision a backend instead?

--- 

## terraform
- decisions
  - do they reprovision everything from p1? yeap, prepare something for them to apply
    - can segregate into folders now too
  - can the beginner track use the same? yeap idealy good to have the same prepared resources
- should sync with prt 2.2 create storage with encrypted KMS
- create launch template (replaces ec2 launch config)
- create ASG
- point ALB to ASG
- setup scaling policies and targets


TODO
- prepare tf files for prepared resources
- prepare instructions to create storage with encrpted kms
- prepare instructions for creating launch template, asg, alb to ASG
  - deploy another app instead?
  - if its backend, we can use promethues /metrics endpoints and traces (or too much?) 


## practical
- demo: terminate one instance manually → watch self-healing
- demo2: run some stress binary inside one of the nodes, see it scale up?
